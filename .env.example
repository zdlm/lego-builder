# Anthropic API key for step understanding and selection
ANTHROPIC_API_KEY=
# Claude model used for vision + ranking (any current vision-capable Claude model)
LEGO_BUILDER_MODEL=
# Where input PDFs and run outputs live
LEGO_BUILDER_DATA_DIR=data
# DPI used when rasterising PDF pages
LEGO_BUILDER_PDF_DPI=200
